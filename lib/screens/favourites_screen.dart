import 'package:flutter/material.dart';

class FavourtiesScreen extends StatelessWidget {
  const FavourtiesScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Something'),
    );
  }
}
